export default class User {
  constructor(id, omeName, firstName, lastName, email) {
    this.id = id;
    this.omeName = omeName;
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
  }
}
